# Ch25｜项目一：工具增强问答 Agent

> **本章目标**：从 0 搭建一个支持 Web 搜索、计算器、文件读取的工具增强问答 Agent。

## 25.1 项目目标

构建一个生产可用的 QA Agent，覆盖：

- ✅ LangGraph 状态机
- ✅ 工具选择准确性
- ✅ 错误恢复
- ✅ 流式输出
- ✅ 高风险工具拦截

## 25.2 技术栈

| 组件 | 选型 |
|---|---|
| LLM | gpt-4o-mini（成本） |
| 框架 | LangGraph |
| 工具 | Tavily（搜索）、Python 解释器、文件读取 |
| 前端 | Streamlit |
| 可观测 | Langfuse |

## 25.3 完整代码

完整代码见 `agent-book-code/ch25/`。核心 `agent.py`：

```python
from typing import TypedDict, Annotated
from langgraph.graph import StateGraph, START, END
from langgraph.graph.message import add_messages
from langgraph.prebuilt import ToolNode
from langgraph.checkpoint import MemorySaver
from langchain_openai import ChatOpenAI
from langchain_core.tools import tool


# State
class State(TypedDict):
    messages: Annotated[list, add_messages]
    iterations: int


# 工具
@tool
def web_search(query: str) -> str:
    """搜索互联网获取最新信息"""
    # TODO: 接入 Tavily
    return f"[搜索结果] {query}"


@tool
def calculator(expression: str) -> str:
    """计算数学表达式"""
    try:
        return str(eval(expression, {"__builtins__": {}}, {}))
    except Exception as e:
        return f"计算错误: {e}"


@tool
def file_reader(path: str) -> str:
    """读取本地文件内容（不超过 2000 字符）"""
    try:
        with open(path) as f:
            return f.read()[:2000]
    except Exception as e:
        return f"读取失败: {e}"


tools = [web_search, calculator, file_reader]
llm = ChatOpenAI(model="gpt-4o-mini").bind_tools(tools)


# 节点
def call_llm(state: State):
    response = llm.invoke(state["messages"])
    return {
        "messages": [response],
        "iterations": state.get("iterations", 0) + 1,
    }


def should_continue(state: State):
    if state["iterations"] > 10:
        return END
    last = state["messages"][-1]
    if hasattr(last, "tool_calls") and last.tool_calls:
        return "tools"
    return END


# 构图
graph = StateGraph(State)
graph.add_node("agent", call_llm)
graph.add_node("tools", ToolNode(tools))
graph.add_edge(START, "agent")
graph.add_conditional_edges("agent", should_continue, {
    "tools": "tools",
    END: END,
})
graph.add_edge("tools", "agent")

memory = MemorySaver()
app = graph.compile(checkpointer=memory)
```

## 25.4 验收指标

| 指标 | 目标 | 实测 |
|---|---|---|
| 工具选择准确率 | > 90% | 待测 |
| 端到端成功率 | > 85% | 待测 |
| P99 延迟 | < 5s | 待测 |

## 25.5 小结

本项目是后续 3 个项目的基础。**重点关注工具描述工程**（Ch4 讲的 4 要素）。
